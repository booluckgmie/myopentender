{
  "installCommand": "npm install --foreground-scripts",
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.js" }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "24"
    }
  }
}
